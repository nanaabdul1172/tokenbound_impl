[package]
name = "royalty_split"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }
upgradeable = { path = "../upgradeable" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
